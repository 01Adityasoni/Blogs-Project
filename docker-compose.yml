services:
  postgres:
    image: postgres:17.4
    ports:
      - "5433:5432"
    env_file:
      - .env

  app:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env 
    depends_on:
      - postgres